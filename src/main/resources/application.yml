spring:
  application:
    name: Resto-Bat

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/restaurant
    username: postgres
    password: rootroot

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true


server:
  error:
    include-stacktrace: always

app:
  jwt:
    secret: nurislam&java12